 R←Test_Options_001(stopFlag batchFlag);⎕TRAP;result;list
⍝ Test setting "messages"
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 R←∆Failed

 ∆ClearLog

⍝ Make sure that the `-messages` option is tolerated until it is finally retired.
⍝ Currently it's just deprecated!

 {}'-messages=on'∆RunUcmd'Projects'
 ∆ClearLog

 R←∆OK
⍝Done
