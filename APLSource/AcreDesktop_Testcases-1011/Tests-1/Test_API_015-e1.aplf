 R←Test_API_015(stopFlag batchFlag);⎕TRAP;F;path;result;buff;openList
⍝ Exercise the API function `Close`.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 R←∆Failed

 openList←⎕SE.acre.Projects'' ⍝ in case was open from prev test

 F←##.FilesAndDirs
 #.⎕SHADOW'TEMP'
 'TEMP'#.⎕NS''
 #.TEMP.Fooü←{42}

 openList←⎕SE.acre.Projects''

 path←{_←F.DeleteFile ⍵ ⋄ ¯4↓⍵}F.GetTempFilename''
 result←∆RunUcmd'CreateProject "',path,'" #.TEMP'

 →GoToTidyUp({⍵[;1]}⎕SE.acre.Projects'')≢openList[;1],⊂result
 →GoToTidyUp(≢openList)≢⎕SE.acre.Close result
 →GoToTidyUp({⍵[;1]}⎕SE.acre.Projects'')≢openList[;1]

 R←∆OK

∆TidyUp:
 ∆RunUcmd'Close #.TEMP'
⍝Done
