 R←Test_Misc_012(stopFlag batchFlag);⎕TRAP;projectName;projectPath;ref;result
⍝ Creates a project that contains nothing but a sub namespace that carries a function
⍝ This must fail due to the fact that they have different file extensions
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 R←∆Failed

 projectName←⊃⎕SI
 projectPath←∆PATH,'/',projectName
 #.⎕SHADOW projectName
 ref←⍎projectName #.⎕NS''
 ref←⍎projectName ref.⎕NS''
 ref.Hello←{42}

 :Trap (~stopFlag)/0
     {}∆RunUcmd'Createproject ',projectPath,' #.',projectName
 :Else
     →GoToTidyUp 1
 :EndTrap

 {}∆RunUcmd'Close #.',projectName
 result←∆RunUcmd'OpenProject ',projectPath,' #.',projectName
 →GoToTidyUp('#.',projectName)≢result
 ref←⍎'#.',projectName
 →GoToTidyUp 0≠≢ref.⎕NL 2 3 4
 ref←ref.⍎projectName
 →GoToTidyUp 1≠≢ref.⎕NL 2 3 4

 R←∆OK

∆TidyUp:
 F.RmDir projectPath
 ∆ClearLog
⍝Done
