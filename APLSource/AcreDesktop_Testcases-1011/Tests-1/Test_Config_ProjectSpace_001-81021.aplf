 R←Test_Config_ProjectSpace_001(stopFlag batchFlag);⎕TRAP;projectPath;ref;projectName;buff;result;list
⍝ Create a project and check whether `acreconfig.txt[ProjectSpace]` is honored.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 R←∆Failed

 projectName←⊃⎕SI
 projectPath←∆PATH,'/',projectName
 #.⎕SHADOW projectName
 ref←⍎projectName #.⎕NS''
 ref.⎕FX'r←Hello' 'r←''World'''

 :Trap (~stopFlag)/0
     result←∆RunUcmd'Createproject "',projectPath,'" #.',projectName
 :Else
     →GoToTidyUp 1
 :EndTrap

 #.⎕EX projectName
 #.⎕SHADOW'Temp'
 buff←''
 buff,←⊂':Namespace'
 buff,←⊂'   ProjectSpace←''#.Temp'''
 buff,←⊂':EndNamespace'
 A.WriteUtf8File(projectPath,'/acreconfig.txt')buff

 :Trap (~stopFlag)/0
     list←∆RunUcmd'Projects '
 :Else
     →GoToTidyUp 1
 :EndTrap

 →GoToTidyUp'#.Temp'≢∆RunUcmd'OpenProject ',projectPath,'  -messages=''off'''
 →GoToTidyUp 1≠(⊃⍴∆RunUcmd'Projects')-⊃⍴list
 →GoToTidyUp'World'≢#.Temp.Hello

 R←∆OK

∆TidyUp:
 F.RmDir projectPath
