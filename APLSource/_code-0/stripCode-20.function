 stripCode←{⍺←⊢
     in out←'#' '#'(⊢,,)¨⍺⊣'/.'
     one←{⍺,⊃'-'sep ⍵}
     (≢out)↓¨(⊢⊂⍨out⍷⊢)⊃,/(⊃out)∘one eu 1↓(⊃in)sep⊃,/in∘,¨⊂if≡⍵
⍝ same as decodeCaps but ignore caseCode leaving caps as are
⍝ Phil Last 2018-01-11 17.17
 }
