 optionFields←{⍺←⊢
     unused←'abefgijklnopqrsuvxyz'
     opts←,⊂'casecode:on'
     opts,←⊂'confirm:'
     opts,←⊂'dependencies:on'
     opts,←⊂'encoding:aplan'
     opts,←⊂'help'
     opts,←⊂'messages:on'
     opts,←⊂'track:on'
     opts,←⊂'when[=]'

     in←3⍴¨opts
     opts←' -'∘,¨opts
     {1↓⊃,/opts[in⍳in∩3⍴¨' 'sep ⍵]}¨⊂if≡⍵
⍝ convert strings of initials to option specs
⍝ Phil 2018-04-03 12.16
 }
