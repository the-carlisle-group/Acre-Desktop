 setAPI←{
     tgt←UT.rs⊢ici←⎕THIS
     fns←↓⍉↑CD.(command type)
     {f←⍎'⍵.',(⊃⍺),'←ici.(',(⊃⍺),' callAPI ⍺)' ⋄ ⍵}/fns,tgt
⍝ ⍺ ?
⍝ ⍵ ?
⍝ ← target space for API fns
⍝ Phil 2019-08-12 21.13 5.2.0+230
 }
