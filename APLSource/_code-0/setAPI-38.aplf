 setAPI←{⍺←⊢ ⋄ name←MS.FY ⋄ df←'[',name,']'
     one←{src←⍺⍺ ⋄ pre←⍵⍵ ⋄ tgt←⍵ ⋄ fn←⍺
         msg←'⋄z←⍵⍵↓⍕⍺,''',df,'.',fn,' '',,⍵⋄'
         fn←'←⍺⍺{0::⎕signal/⌽⎕en,⎕dm⋄⍺←⊢',msg,'⍺ ⍺⍺.',fn,'⍵}⍵⍵'
         z←tgt.⍎⍺,fn
         tgt
     }
     s←1⊃⎕SE name _is_(API one runAPI)/⌽(⎕SE.⎕NS''),~∘' '¨↓API.⎕NL 3.2
     s.⎕DF df
⍝ specifically ⎕NL 3.2 to exclude ops and dervs
⍝ Phil 2019-08-06 21.09 5.2.0+226
 }
