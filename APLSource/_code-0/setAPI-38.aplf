 setAPI←{⍺←⊢
     API{src←⍺⍺ ⋄ tgt←⍵
         z←tgt.⍎⍺,'←⍺⍺{0::⎕signal/⌽⎕en,⎕dm⋄⍺←⊢⋄⍺ ⍺⍺.',⍺,'⍵}'
         tgt
     }/⌽(⎕SE.⎕NS''),↓API.⎕NL 3.2
⍝ specifically ⎕NL 3.2 to exclude ops and dervs
⍝ Phil 2019-03-15 12.44
 }
