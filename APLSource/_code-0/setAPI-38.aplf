 setAPI←{⍺←⊢ ⋄ name←MS.FY ⋄ df←'[',name,']'
     one←{src←⍺⍺ ⋄ log←⍵⍵ ⋄ tgt←⍵ ⋄ fn←⍺
         msg←'⋄z←⍵⍵↓⍕⍺,''',df,'.',fn,' '',,⍵⋄'
         fn←'←⍺⍺{0::⎕signal/⌽⎕en,⎕dm⋄⍺←⊢',msg,'⍺ ⍺⍺.',fn,'⍵}⍵⍵'
         z←tgt.⍎⍺,fn
         tgt
     }
     z←⎕SE name _is_(API one logMsg)/⌽(⎕SE.⎕NS''),~∘' '¨↓API.⎕NL 3.2
     {⍵.⎕DF df}⎕SE.⍎name
⍝ specifically ⎕NL 3.2 to exclude ops and dervs
⍝ Phil 2019-05-29 06.21
 }
