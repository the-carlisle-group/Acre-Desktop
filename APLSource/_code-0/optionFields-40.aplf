 optionFields←{⍺←⊢
     opts←,dflt←,⊂'help'
     opts,←'track:on' 'confirm:' 'dependencies:on'
     opts,←'casecode:off' 'when[=]' 'keephistory:off'

     len←3 ⍝ min unique width of above
     in←len⍴¨opts
     opts←' -'∘,¨opts
     {1↓⊃,/opts[in⍳in∩len⍴¨dflt,' 'sep ⍵]}¨⊂if≡⍵
⍝ convert strings of initial abbreviations to option specs
⍝ see cmdDefs
⍝ Phil 2018-10-29 14.07
 }
