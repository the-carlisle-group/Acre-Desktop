 Start←{
     path←⍵
⍝     setEnvironment∘setDataSpaces if newSession path
     setEnvironment∘setDataSpaces path
⍝ ⍵ used by setEnvironment unchanged
⍝ get things going once per session
⍝ ← did we need to
 }
