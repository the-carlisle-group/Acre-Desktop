 test←{⍺←⊢ ⋄ arg←⍵
     z←refs''
     tests←##.⍎MS.FZ
     fns←tests.Tests.⎕NL-⍳11
     z←tests.(Tester.EstablishHelpersIn Tests)
     z←{tests.Tests.Run⊣'nilad'}if null arg
     z←{tests.Tests.RunThese ⍵}if nonul arg
     z←tests.Tests.{⎕EX(⎕NL-⍳11)~⍵}fns
⍝ ⍵ null - run all cleanly
⍝   'group' (n n ...) - run these in debug mode
⍝ Phil 2019-10-15 12.24 6.0.2+279
 }
