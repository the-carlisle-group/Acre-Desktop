 runAPI←{⍺←⊢
     z←⎕SE.UCMD if(newSession'')⊢'acre.log'
     z←openProjects''
     setSpace logMsg ⍵
⍝ pre-processor for API fns
⍝ ⍵ log msg
⍝   if new session refresh code and setup anew
⍝   logs call
⍝   sets calling space
⍝ ← ??
⍝ Phil 2019-07-22 23.14 5.2.0+209
 }
