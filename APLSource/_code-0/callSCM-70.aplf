 callSCM←{dat←⍺
     exit←dat.SCMexit
     fqn old new path←⍵
     ⍺(#.⍎⍣(3∊#.⎕NC exit)⊢exit)⍵
⍝ calls external routine with ⍵ to override acme's own
⍝ ⍺ dat
⍝ ⍵ fqn old new path
⍝ ←    0: exit with 0 - fails to leave editor
⍝      1: exit with 1 - leave editor - assume handled by SCM
⍝   else: continue with normal processing
⍝ Phil 2019-05-02 20.20
 }
