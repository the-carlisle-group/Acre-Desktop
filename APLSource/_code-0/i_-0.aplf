 i_←{⍺←⊢
     16≤⊃SV.aplver:⍺⍸⍵
     1≡⍺ 1:(,⍵)/,⍳⍴⍵              ⍝ m: where
     ¯1+(≢⍺)↓i⊣i[i]←+\(≢⍺)>i←⍋⍺⍪⍵ ⍝ d: interval index
⍝  - see setEmulators
⍝ interval index - Roger Hui - http://www.jsoftware.com/papers/50/
⍝ Phil 2019-03-11 10.57
 }
