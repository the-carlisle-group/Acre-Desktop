 _NPARTS←{⍺←⊢
     15≤⊃SV.aplver:⊢⍺ ⎕NPARTS ⍵
     ch←a_(∊∘'/\')
     p f←(+/∨\⌽⍵∊'/\')(↑__↓)⍵
     p←'/'ch p
     f e←{(-1+⊥⍨⍵≠'.')(↓__↑)⍵}∘⊃if('.'∊1↓f)⊢f''
     0=⍺⊣0:p f e
     b←(':'∊p)⍱'//'≡2↑p
     p←{⍵/⍨1,1↓~'//'⍷⍵}'/'ch{(⊃⎕CMD'cd'),'\',⍵}if b⊢p
     ~∨/'./'⍷p:p f e
     p←1⌽⊃,/'/',¨~∘(⊂'..'){⍵/⍨(⊢⍱¯1⌽⊢)2 0⍷'..' ''⍳⍵}⍣≡~∘(⊂,'.')(≠∘'/'c_⊢)p
     p f e

⍝  - see setEmulators
⍝ Phil 2018-12-30 23.02
 }
