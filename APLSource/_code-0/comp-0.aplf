 comp←{⍺←⊢
     cmd←819⌶Cmd←(cmdDefs'').command
     fns←~∘' '¨↓API.⎕NL 3
     all←{⍵[⍋⍵]}cmd∪fns
⍝     ⊃,∘⍪/all∘(⊢(\⍨)∊)¨cmd fns
     ⊃,∘⍪/(all∘∊¨cmd fns)\¨Cmd fns
⍝
⍝ Phil 2019-07-04 14:41 5.2.0+204
 }
