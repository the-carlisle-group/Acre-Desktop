 logMsg←{⍺←⊢
     null⊢msgs←ntbs if≡⍵:msgs
     head←nout{⍵(⍵⊢¨'=')}⍺⊣''
     data←head,msgs
     SV.errmsgs,←data/⍨'!'=⊃¨data  ⍝ reset in userCmdRun
     ⍵⊣MP.log{(⊢↑⍨1000⌊≢)⍵,⍺}←data

⍝   MP←{⍵⊣⍵._←,¨''}⎕SE.⎕NS'' ⍝ setEnvironment
⍝ [⍺]   header to be underlined if present
⍝  ⍵    msg(s) string, list or mat
⍝  ←    ⍵ having displayed in log window
⍝ Phil 2018-11-14 15.58
 }
