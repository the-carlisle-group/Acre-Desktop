 FromAPLAN←{
     null ⍵:''
     PL.decode #.⍎if(2=⎕NC)⊃∘prefSpace if(0≤⎕NC)⊃(⊣,' ',⊢)/⍵
⍝ decode APLAN expression as array or space
⍝ see ToAPLAN
⍝ expression requires "double quotes" if it contains [brackets].
⍝ Phil 2018-01-25 08.05
 }
