 pt←{
     (⍺⍺⊣⍵){(⍺/2</0,⍺)⊂⍺/⍵}⍵
⍝ partition
⍝ ⍺⍺ boolean string or fn to return such given ⍵
⍝ ⍵  string
⍝ ← ⍵ partitioned each partition being ⍵ corresponding
⍝   to a contiguous string of 1s in ⍺⍺⊣⍵
⍝ Phil Last ⍝ 2008-02-12 07:51
 }
