 fgh←{⍺←⊢
     #.(f←{⍺←⊢ ⋄ '(',⍺,' f ',⍵,')'})
     #.(g←{⍺←⊢ ⋄ '(',⍺,' g ',⍵,')'})
     #.(h←{⍺←⊢ ⋄ '(',⍺,' h ',⍵,')'})
     #.(i←{⍺←⊢ ⋄ '(',⍺,' i ',⍵,')'})
     #.(j←{⍺←⊢ ⋄ '(',⍺,' j ',⍵,')'})
⍝
 }
